# TODO

> Закрытые задачи → [`DONE.md`](DONE.md) · Сырые находки от review/audit → [`BACKLOG.md`](BACKLOG.md)

---

## Audit Scope

<!--
  Читается backend-audit и frontend-audit в фазе INVENTORY.
  Перечисли что исключать из анализа при следующем аудите.
-->

- **[Область]** (`path/to/`) — причина исключения

---

## Backend

<!--
  Engineer читает этот раздел как источник задач.
  Задача закрыта когда все чекбоксы отмечены → переносить в DONE.md.

  Формат одной задачи:
  **[ID] `Class::method()` — [заголовок]**
  > [file:line] — [описание проблемы и риска]
  ```lang
  // fix
  ```
  - [ ] [шаг]
  - [ ] **Проверка:** [сценарий → ожидаемый результат]
-->

**B.N `Class::method()` — [заголовок]**

> `path/to/File.php:NN` — [описание проблемы, риск].

```php
// Было:
old_code();
// Стало:
new_code();
```

- [ ] [Дополнительный шаг если нужен]
- [ ] **Проверка:** [сценарий → ожидаемый результат]

---

## CI/CD

**B.N `filename.conf` — [заголовок]**

> `scripts/file.conf:NN` — [описание].

```nginx
# Исправление
```

- [ ] **Проверка:** [команда] → [ожидаемый результат]

---

## Frontend

**F.N `file.ts` — [заголовок]**

> `path/to/file.ts:NN` — [описание].

```ts
// Исправление
```

- [ ] **Проверка:** [браузер / мобайл / граничный случай]
